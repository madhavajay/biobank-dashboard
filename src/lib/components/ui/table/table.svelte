<script lang="ts">
	import type { HTMLTableAttributes } from "svelte/elements";
	import { cn, type WithElementRef } from "$lib/utils.js";

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: WithElementRef<HTMLTableAttributes> = $props();
</script>

<div
	data-slot="table-container"
	class="relative w-full overflow-x-auto rounded-[1rem] border border-[#d8e5ea] bg-white/68 shadow-[inset_0_1px_0_rgba(255,255,255,0.66)]"
>
	<table bind:this={ref} data-slot="table" class={cn("w-full caption-bottom text-[13px]", className)} {...restProps}>
		{@render children?.()}
	</table>
</div>
